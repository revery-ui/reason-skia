(rule
    (targets cclib_c_library_flags.sexp)
    (deps (:discover ../src/config/discover.exe))
    (action (run %{discover})))

(executable
 (name TestApp)
 (public_name TestApp.exe)
 (flags (:include cclib_c_library_flags.sexp))
 (libraries skia))
