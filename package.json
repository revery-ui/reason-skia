{
  "name": "reason-skia",
  "version": "0.1.0",
  "description": "Reason bindings for the Skia 2D graphics library",
  "esy": {
    "build": [
      [
        "refmterr",
        "dune",
        "build",
        "--root",
        "."
      ]
    ],
    "install": [
      "esy-installer"
    ]
  },
  "peerDependencies": {
    "ocaml": "~4.6.0"
  },
  "dependencies": {
    "@esy-ocaml/reason": "^3.3.7",
    "@opam/dune": "*",
    "esy-skia": "^0.1.6",
    "refmterr": "^3.1.0"
  },
  "devDependencies": {
    "ocaml": "~4.6.0"
  }
}
